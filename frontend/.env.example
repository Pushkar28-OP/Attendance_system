# Leave blank for local Vite development so /api is proxied to the backend.
# Set this only when the frontend is deployed separately from the API.
VITE_API_URL=
VITE_BACKEND_URL=http://127.0.0.1:8000
